{
  "name": "meal-api",
  "version": "1.0.0",
  "description": "Simple API to serve db.json",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
